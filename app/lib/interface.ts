export interface BlogCard {
  title: string;
  smallDescription: string;
  currentSlug: string;
  titleImage: any;
}

export interface SingleBlog {
  currentSlug: string;
  title: string;
  content: any;
  titleImage: string;
}
